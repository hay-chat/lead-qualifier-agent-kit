# Local daemon — the Chrome extension and the agents both use these.
DAEMON_PORT=7823
BASIC_AUTH_USER=icp
BASIC_AUTH_PASSWORD=change-me-setup-generates-one

# 64-char hex string (32 bytes). setup.sh generates one for you.
ENCRYPTION_KEY=
